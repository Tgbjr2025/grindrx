# FIX_NOTES — v0.1.9 audit ship (2026-06-12)

Operator: **Tom**. Committer: `dominus`. Branch: `audit/v0.1.9-fixes`. **NOT pushed** (R11).

## Rollback (R9)
- Rollback tag: `audit-v0.1.9-rollback-20260612` → commit `45083f2` (pre-ship HEAD).
- To undo the ship: `git reset --hard audit-v0.1.9-rollback-20260612`
  (then re-apply the working-tree gradle changes if you still want to build locally).

## What shipped — commit `28b1648`
Finalized the WIP v0.1.9 audit branch + targeted audit fixes:
- **Schema-drift robustness:** cascade grid `v3.ts` and conversation `messages.ts`
  now parse items individually and drop/degrade one bad record instead of blanking
  the whole grid/chat. `album.ts`/`message.ts` allow null cover/thumb/url. (+`v3.test.ts`)
- **Authed-media black-box fix:** new `utils/authed-image.ts`; `ImageMessage` and
  `AlbumMessage` resolve authed `cdns.grindr.com` URLs to `data:` URLs for the
  lightbox + dimension probing. Fixes fullscreen black boxes (sent + received,
  photos + albums + album video).
- **headers.rs:** spoof app version `26.7.0.159416` → `26.9.1.163471`.
- **ws.rs:** `maybe_notify` compares `senderId` as string OR number (no self-notify).
- **Branding:** GrindX → GrindrX (notification title, channel description, log tags).
  Internal channel id `grindx_messages` left unchanged (shared with `MainActivity.kt`).
- **android manifest:** READ_MEDIA_IMAGES/VIDEO + maxSdk32 READ_EXTERNAL_STORAGE.
- **eslint.config.js:** global ignores for generated output + `lint` script.
- **flake.nix:** Linux desktop libs (glib/gtk3/webkit2gtk/…) in the dev shell so
  `bun run test` (incl. `cargo test`) runs on this headless host. Gated to Linux.
- Version `0.1.8` → `0.1.9` (package.json, tauri.conf.json, Cargo.toml, Cargo.lock).

## Validation
- `svelte-check`: **0 errors**
- `vitest`: **43/43 pass**
- `cargo test --lib` (host, via the new flake devshell): **pass**
- `cargo clippy --target aarch64-linux-android --lib`: **0 warnings**
- `eslint`: extremely slow on this host (type-aware `projectService`); non-blocking —
  no `src/` files were changed by the targeted fixes, so lint == baseline.

## Intentionally NOT committed (left dirty in the working tree)
- `src-tauri/gen/android/tauri.settings.gradle`
- `src-tauri/gen/android/app/tauri.build.gradle.kts`
  Both are autogenerated and machine-specific (`/home/ubuntu/…` vs
  `/Users/thomasbateman/…`). Committing them would break the macOS build. Kept dirty
  so this server can build; regenerated by `tauri android` per machine.
  **(STILL DIRTY ON PURPOSE as of 2026-06-18 04:16 UTC — unchanged.)**

## Pending (handed back to Tom)
- **App icon:** 6 concepts presented (A–F); awaiting Tom's pick → `bun run gen:icons`
  → commit → fold into the build.
- **Build:** unsigned debug APK via `nix run .#build-android` (R21). Not yet built.
- **Install:** adb over Tailscale to S26 Ultra (`100.64.176.13`) — phone **offline**
  (last seen ~5h ago). Needs wireless debugging on + phone online.
- No signing (R22). No push (R11).

## Backups (off-tree)
- Pre-edit source backup: `/tmp/audit/pre_edit_backup_20260612_170848.tgz`
- flake.nix pre-edit: `/tmp/audit/flake.nix.bak.pre_audit_20260612_170722`

---

## FOLLOW-UP STATUS (appended 2026-06-18 03:58 UTC — history preserved)

The "Pending" items above were all resolved later the same day / over the following days:
- **App icon — DONE.** Tom picked concept **A** (monogram G); `gen:icons` regenerated all assets;
  committed `d3c0392`.
- **Build — DONE (debug).** v0.1.9 vc1017 arm64 debug APK built via the Nix path.
- **Install — DONE.** Installed to the S26 Ultra over Tailscale adb (`100.64.176.13:5555`); verified
  versionName 0.1.9. (Uninstall wiped app data → Tom re-login.)
- Still NOT pushed (R11). Signing (R22) still NOT done — release apk is still pending.

**Subsequent work (post-v0.1.9) is documented separately in `memory/FIX_NOTES_media_features.md`:**
the media-compat batch (`eaf60dc`), the 3 new features + map-tile CSP (`1d09c10`), and the grid
viewport-windowing perf fix (`03f88f2`, current HEAD). See that file for the open issues that those
commits address or leave open (saved-photo 400, album-share unlock, image-memory freeze, WS DNS).

## SECOND FOLLOW-UP (appended 2026-06-18 04:16 UTC — history preserved)

Note for completeness: as of 04:16 UTC the working tree carries five additional UNCOMMITTED audit
fixes beyond the two gradle files above — `rest.rs` (token-leak hardening), `album.ts` (album-share
unlock), `messages.ts` (dead-import), `grid-state.svelte.ts` (Explore routing), and
`conversation-state.svelte.ts` (WS-leak + self-read guard). These are NOT part of the v0.1.9 ship;
they are documented in `memory/FIX_NOTES_media_features.md` §4 and in SESSION_STATE's dirty-set.
They have no rollback tag yet — tag before any commit/ship (R9/R10).
